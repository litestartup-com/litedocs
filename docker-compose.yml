services:
  litedocs:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./my-docs:/docs
    command: ["--host", "0.0.0.0", "--port", "8000", "--no-reload", "/docs"]

  # Multi-doc example: mount multiple doc directories
  # litedocs-multi:
  #   build: .
  #   ports:
  #     - "8000:8000"
  #   volumes:
  #     - ./docs-api:/docs/docs-api
  #     - ./docs-guide:/docs/docs-guide
  #   command: ["--host", "0.0.0.0", "--port", "8000", "--no-reload", "/docs/docs-api", "/docs/docs-guide"]
